build/
build-*/
deps/SDL/
venv/
__pycache__/
*.pyc

# Generated from assets/*.svg by tools/render_assets.py.
# Tracked deliberately: the CI Windows and macOS runners have no SVG
# rasteriser guaranteed, and the SVG remains the source of truth.
!assets/*.png

# Windows CPython kit: ~26MB of prebuilt runtime, assembled locally.
# See docs/cross-compile.md
deps/winpy/

# Release archives assembled by cmake --target package.
dist/
